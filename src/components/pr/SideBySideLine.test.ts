import { describe, it, expect } from 'vitest'
import { SideBySideLine } from './SideBySideLine'

describe('SideBySideLine', () => {
  it('is exported as a function component', () => {
    expect(typeof SideBySideLine).toBe('function')
  })
})
